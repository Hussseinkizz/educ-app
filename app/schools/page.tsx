import AppShell from "../components/AppShell";

interface Props {}

const SchoolsPage = (props: Props) => {
  return (
    <AppShell>
      <h2 className="flex">Schools Page</h2>
    </AppShell>
  );
};

export default SchoolsPage;
