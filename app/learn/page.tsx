import AppShell from "../components/AppShell";

interface Props {}

const LearnPage = (props: Props) => {
  return (
    <AppShell>
      <h2 className="flex">Learn Page</h2>
    </AppShell>
  );
};

export default LearnPage;
