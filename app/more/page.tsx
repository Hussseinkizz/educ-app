import AppShell from "../components/AppShell";

interface Props {}

const MorePage = (props: Props) => {
  return (
    <AppShell>
      <h2 className="flex">More Page</h2>
    </AppShell>
  );
};

export default MorePage;
